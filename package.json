{
  "name": "optiline",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Certified intrinsic-curvature racing-line optimizer (static WebGPU app).",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node tools/stage-github-pages.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run",
    "test:e2e": "playwright test",
    "test": "npm run typecheck && npm run test:unit && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.13.3",
    "@webgpu/types": "^0.1.64",
    "typescript": "~7.0.0",
    "vite": "~8.2.0",
    "vitest": "^3.2.0"
  }
}
